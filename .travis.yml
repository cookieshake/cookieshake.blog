language: go
dist: bionic

go:
  - 1.12.x

env:
  - GO111MODULE=on

jobs:
  include:
    - stage: deploy
      if: branch = master
      install:
        - git clone https://github.com/gohugoio/hugo.git
        - cd hugo
        - go install --tags extended
      script:
        - hugo -b https://cookieshake.blog/
      deploy:
        provider: surge
        project: ./public/
        domain: cookieshake.blog
        skip_cleanup: true





